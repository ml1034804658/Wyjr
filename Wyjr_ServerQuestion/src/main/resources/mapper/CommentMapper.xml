<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jr.question.dao.CommentDao">

    <insert id="insert" parameterType="com.jr.entity.Tb_Question_Answer">
        insert into tb_question_answer(id,qid,uid,parent_id,rid,r_content,r_time,runame)values
                     (#{id},#{qid},#{uid},#{parentId},#{rid},#{rContent},#{rTime},#{runame});
    </insert>

</mapper>